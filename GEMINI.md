# Инструкции для проекта Zero-Kelvin-Stazis

## 1. Роль и Цель

Ты — **Senior Rust Developer** и эксперт по системному администрированию Linux (специализация: Arch Linux). Твоя задача — портировать функциональность утилит резервного копирования `zero-kelvin-store` (zks) и `squash_manager` с языка Fish Shell на **Rust**.

## 2. Описание оригинальных fish-функций
Это CLI-утилита для **выгрузки данных** (Cold Storage) в среде Arch Linux.
В отличие от традиционных систем резервного копирования (цель которых — избыточность), `zks` предназначен для «заморозки» пользовательских данных: переноса их с локального хоста на внешнее хранилище (NAS/HDD) для освобождения дискового пространства, сохраняя при этом возможность легко «разморозить» (восстановить) их обратно.

**Цель проекта:** Создать надежную, быструю и безопасную CLI-утилиту для создания оффлоадов (полных сжатых бэкапов), использующую технологии SquashFS, ZSTD для сжатия, LUKS-шифрование (опционально) и Rclone для создания sqfs-offload'ов сразу на сетевых хранилищах без монтирования сетевых каталогов т.к. передачах данных по сети через смонтированный сетевой каталог происходит медленнее  (это тоже опциональная возможность и взаимоисключающая с luks-шифрованием т.к. использовать сразу и то и другое одновременно технически не получится).

**Философия и цели:**
* **Offloading (Выгрузка):** Концептуально это перемещение данных, а не просто копирование.
    * *Техническая реализация:* Данные копируются в образ SquashFS, проверяются, и только после успешного создания архива исходные файлы удаляются с локального диска (опционально, по команде пользователя или флагом, по-умолчанию не удаляются, остаются для проверки и удаления вручную ).
* ** Опционально: Zero-Knowledge (Приватность):** Архивы должны быть «черными ящиками». Без ключа дешифрования ни метаданные, ни имена файлов, ни структура каталогов не должны быть видны.
    * *Реализация:* Сама `zks` не реализует криптографию. Она делегирует эту задачу зависимости `squash_manager`, передавая ей флаг `-e` (или `--encrypt`), только если такой же флаг был передан самой `zks`.
* **Contextual Sets (Контекстные наборы):** Возможность сгруппировать разрозненные пути (например, `/home/user/project` + `/etc/project.conf`) под одним идентификатором архива (ID).
* **Low Friction (Минимум трения):** Процесс восстановления должен быть автоматизированным. Утилита сама знает исходные пути файлов благодаря внутреннему манифесту.
* **Мгновенный доступ:** Использование SquashFS позволяет примонтировать архив и просмотреть содержимое без полной распаковки.

### Портирование
* **Fish-скрипты:** Логика работы (флаги `mksquashfs`, алгоритм именования файлов) находится в папке `to_port/`. Используй их как "Reference Implementation", но не копируй слепо — адаптируй под идиомы Rust. При портировании не Rust немного меняется название: zero-kelvin-store -> zero-kelvin-stazis

## Исходники fish-функции, на которые ты должен ориентироваться при портировании
- [Конечная утилита zero-kelvin-store](to_port/zero-kelvin-store.fish)
- [Вспомогательная утилита squash_manager.fish](to_port/squash_manager.fish)
- [Безопасная альтернатива rm -rf: safe_remove.fish](to_port/safe_remove.fish)
- [Обвёртка над утилитами для повшения привилегий: get_root_cmd.fish](to_port/get_root_cmd.fish) 

последнию необязательно портировать "дословно", можно реализовать аналогичную функциональность на Rust для учёта того что вместо `sudo` пользователь может использовать `doas`,`sudo-rs` или: `run0`, но с учётом идиом и возможностей языка Rust.

### Флаги команды
Данные флаги и команды должны быть портированы на Rust в неизменном виде, для обеспечения полной обратной совместимости:

```
fireice@katana ~/src/rust/Zero-Kelvin-Stazis (dev) 
🦀🐟 squash_manager --help
Usage: squash_manager create [OPTIONS] <input_path> [output_path]
       squash_manager mount <image> <mount_point>
       squash_manager umount <mount_point>

Options for 'create':
  -e, --encrypt         Create an encrypted LUKS container
  -c, --compression=N   Zstd compression level (default: 15)
  --no-progress         Disable progress bar

Description:
  Converts a directory OR an archive (tar.zst, zip, 7z, etc.) into SquashFS.
  With -e, it creates a LUKS container and streams data without cleartext on disk.
fireice@katana ~/src/rust/Zero-Kelvin-Stazis (dev) 
🦀🐟 zero-kelvin-store --help
Usage: zero-kelvin-store (zks) <command> [options]

Commands:
  freeze [targets...] [archive_path]    Offload data to a SquashFS archive
                                        If [archive_path] is a directory, prompts for filename

  unfreeze <archive_path>               Restore data from an archive

  check <archive_path>                  Verify archive integrity vs live system

Freeze Options:
  -e, --encrypt                         Encrypt the archive using LUKS (via squash_manager)
  -r, --read <file>                     Read list of targets from a file

Check Options:
  --use-cmp                             Verify file content (byte-by-byte) in addition to size
  --force-delete                        Delete local files if they match the archive (Destructive!)
Examples:
  zks freeze /home/user/project /mnt/nas/data/backup.sqfs
  zks freeze -e /secret/data /mnt/nas/data/secret.sqfs_luks.img
  zks unfreeze /mnt/nas/data/backup.sqfs

```
чтобы не было путаницы с уже существующими в системе командами, бинарники портированной версии должны называться zks-rs и squash_manager-rs

### Структура создаваемых файлов
 - файл, созданный оригинальной функцией zero-kelvin-store.fish должен откываться портом zks-rs
 - файл, созданный  портом zks-rs должен откываться оригинальной функцией zero-kelvin-store.fish
 - тоже самое касается и вспомогательной утилиты squash_manager

#### Структура дерева файлов и катологов внутри создаваемых squashfs-образов
Sqfs-образ, который создаётся при помощи zks/zks-rs, должен иметь следующую структуру дерева файлов и каталогов:
```
.
├── list.yaml
└── to_restore
    ├── 1
    │   └── etc
    │        ├── classic
    │        ├── geth
    │        └── keystore
    ├── 2    
    │   └── eth-vanil
    │        ├── geth
    │        └── keystore
    ├── 3
    │   └── ethw
    │        ├── geth
    │        └── keystore
    └── 4
        └── mnr
            ├── bitmonero.log
            ├── lmdb
            ├── p2pstate.bin
            ├── rpc_ssl.crt
            └── rpc_ssl.key
```

list.yaml, to_restore, 1, 2, 3, 4 и т.д. -- фиксированные имена, все остальные для примера.

#### Формат yaml-файла
```
metadata:
  date: "Tue Jan 27 08:09:58 PM +04 2026"
  host: "katana"
files:
  - id: 1
    name: "etc"
    restore_path: "/home/user/data/chains"
    type: directory
  - id: 2
    name: "eth-vanil"
    restore_path:  "/home/user/data/chains"
    type: directory
  - id: 3
    name: "ethw"
    restore_path: "/home/user/data/chains"
    type: directory
  - id: 4
    name: "mnr"
    restore_path: "/home/user/data/chains"
    type: directory
```

#### Legacy-формат
Данный формат должен также корректно обрабатываться коммандами unfreeze и check, но командой freeze он не создаётся
```
metadata:
  date: "Tue Jan 27 08:09:58 PM +04 2026"
  host: "katana"
files:
  - id: 1
    original_path: "/home/share/data/extra/chains/etc"
    type: directory
  - id: 2
    original_path: "/home/share/data/extra/chains/eth-vanil"
    type: directory
  - id: 3
    original_path: "/home/share/data/extra/chains/ethw"
    type: directory
  - id: 4
    original_path: "/home/share/data/extra/chains/mnr"
    type: directory

```

```
.
├── list.yaml
└── to_restore
    ├── 1
    │   ├── classic
    │   ├── geth
    │   └── keystore
    ├── 2
    │   ├── geth
    │   └── keystore
    ├── 3
    │   ├── geth
    │   └── keystore
    └── 4
        ├── bitmonero.log
        ├── lmdb
        ├── p2pstate.bin
        ├── rpc_ssl.crt
        └── rpc_ssl.key

```


### Философия
#### Offloading (Выгрузка), а не Backup (Резервирование)

Это  ключевое отличие от стандартных решений (TimeShift, Borg, Restic).

- **Цель:** Освобождение дискового пространства на ноутбуке (Arch Linux).
- **Суть:** Не столько резервные копии (хотя на NAS с RAID5-HDD хранить всё-равно безопаснее, чем на SSD ноута), сколько «замораживание» проектов или данных, которые **не нужны прямо сейчас**, но могут понадобиться в будущем.
- **Действие:** Данные переносятся на внешний носитель (NAS или HDD) с локального диска.
#### Минимизация «телодвижений» (Low Friction Restore)

- **Цель:** Восстановление данных должно быть почти таким же простым, как `Ctrl+Z` (образно говоря).
- **Суть:** Утилита должна сама помнить, **откуда** были взяты файлы.
- **Решение:**
    - Пользователь не должен вручную указывать: «Распакуй этот архив в `/home/user/project`».
    - Утилита читает внутренний манифест (`list.yaml`) и сама предлагает: «Этот архив содержит файлы из `/etc/nginx` и `/home/user`. Вернуть их на место?».
    - Использование уникальных ID (имя хоста + дата + тег) позволяет быстро находить нужный "слепок" системы.
#### Гибкость и Атомарность (Contextual Sets)

- **Цель:** Умение работать с логически связанными, но физически разрозненными данными.
- **Суть:** Проект — это часто не одна папка. Это «код в `/home` + конфиг в `/etc` + база в `/var`»
- **Требование:** Утилита должна уметь собрать эти разбросанные пути в **один** архивный файл. И при восстановлении разложить их обратно по полочкам, не создавая конфликтов имен (решено через структуру нумерованных контейнеров `1/`, `2/`).

#### User-Space Friendly (Права доступа)

- **Цель:** Не использовать повышение привилегий (`sudo`/`doas`) там, где это не нужно.
- **Суть:** Если вы восстанавливаете свои личные файлы в домашнюю папку, утилита не должна требовать root-прав. Root нужен только для системных путей или монтирования (хотя монтирование через `udisks` или `squashfuse` в будущем могло бы решить и это, но пока мы используем `sudo` для `mount`).
#### KISS и Native (Arch Way)

- **Цель:** Простота, прозрачность и интеграция.
- **Суть:**
    - Никаких демонов, висящих в фоне.
    - Никаких сложных баз данных (всё хранится в файлах и внутри самих архивов).
    - Написано на **Fish**, для быстрого доступа к функционалу из данной современной оболочки.
    - Использование стандартных Linux-инструментов (`rsync`, `mksquashfs`, `cryptsetup`), чтобы данные можно было восстановить «руками», даже если утилита `zks` исчезнет.
        
**Итог одной фразой:**

Нужно создать **персональную, шифрованную «криокамеру»** для проектов и данных, которая позволяет одним движением освобождать место на ноутбуке и так же легко возвращать всё обратно, сохраняя структуру путей и права доступа.

## 3. Порт на Rust

### Отличия порта от реализации на fish

Хотя порт должен сохранять обратную совместимость по флагам CLI и структуре архивов, внутренняя архитектура работы с файловой системой и привилегиями кардинально меняется.

Главная цель изменений: **Работа без Root (Rootless Freeze)** всегда, когда это возможно. Права суперпользователя могут потребоваться для восстановления системных файлов или для архивации файлов, недоступных для чтения, но не для работы с обычными пользовательскими файлами.

#### 1. Архитектура "User Namespace Bind Strategy"

В отличие от Fish-версии, которая использовала `sudo unshare -m` (требуя пароль root для создания bind-mounts), Rust-версия будет использовать нативные возможности ядра Linux — **User Namespaces** (`CLONE_NEWUSER`).

* **Fish (Legacy):** Полагалась на SUID-бинарники (`sudo`, `mount`).
* **Rust (Target):** Бинарник `zks-rs` должен самостоятельно создавать изолированное окружение.
    * Процесс создает новый User Namespace, маппинг текущего пользователя (`uid`) в `root` (uid 0) **внутри** пространства имен.
    * Это дает процессу capability `CAP_SYS_ADMIN` **внутри контейнера**, что позволяет выполнять системный вызов `mount --bind` без реальных прав суперпользователя в хост-системе.

#### 2. Смена "Сборочного цеха" (Staging Area)

Мы отказываемся от использования глобальной папки `/tmp` для сборки структуры архива в пользу пользовательского кэша.

* **Fish (Legacy):** Использовала `/tmp/zks_build_<uuid>`. Это небезопасно (доступно другим пользователям) и захламляет системный tmp.
* **Rust (Target):**
    * Используй директорию согласно XDG Base Directory: `$XDG_CACHE_HOME/zero-kelvin-stazis/build_<unix-time>_<random>/` (обычно `~/.cache/zero-kelvin-stazis/...`).
    * Эта папка принадлежит пользователю, не требует `sudo` для записи и скрыта от других глаз.

#### 3. Алгоритм сборки (Stub & Bind)

**НИ ПРИ КАКИХ ОБСТОЯТЕЛЬСТВАХ** не используем сборку каталога через копирование (`cp`) или хардлинки (`ln`). Мы используем стратегию **"Заглушка и Монтирование"**. Это обеспечивает **Zero-Copy** (нулевое копирование данных) и мгновенную скорость работы даже между разными физическими дисками.

**Пошаговый алгоритм реализации Freeze:**

1.  **Подготовка (Host Namespace):**
    * `zks-rs` парсит аргументы и строит список целей.
    * В папке `$XDG_CACHE_HOME/.../to_restore/` воссоздается дерево каталогов.
    * Для каждого целевого файла создается **пустой файл-заглушка** (`touch`).
    * Для каждой целевой директории создается **пустая папка**.
    * *На этом этапе данные не читаются, место на диске не тратится.*

2.  **Изоляция (Enter Namespace):**
    * `zks-rs` делает `unshare(CLONE_NEWUSER | CLONE_NEWNS)`.
    * Теперь процесс "думает", что он root.

3.  **Связывание (Bind Mounts):**
    * Процесс пробегается по списку заглушек и выполняет `mount --bind /real/path /cache/stub`.
    * Теперь в папке кэша вместо пустых заглушек "магически" появляется реальное содержимое файлов.

4.  **Упаковка:**
    * Запускается `mksquashfs` на папку кэша. Утилита видит реальные данные через точки монтирования.

5.  **Очистка:**
    * При выходе из процесса namespace уничтожается ядром. Все точки монтирования исчезают автоматически.
    * Остается только удалить папку с пустыми заглушками в кэше. Это быстро и безопасно, но на всякий случай мы используем логику, аналогичную `rm-if-empty` (удаляем только если внутри нет реальных данных, чтобы случайно не задеть файлы пользователя, если namespace не отмонтировался).

#### 4. Обработка привилегий (Sudo)

* **Режим Plain (без шифрования):** Может работать **полностью без sudo**, если у пользователя есть права на чтение исходных файлов.
* **Режим LUKS (`-e`):** Поскольку `cryptsetup` требует доступа к Device Mapper (что обычно требует реального root), в этом режиме запрос прав суперпользователя допустим (но только для этапа шифрования, сборка squashfs всё равно должна идти в namespace). *Примечание: В будущем возможен переход на userspace crypto, но пока используем cryptsetup.*

При запуске от имени пользователя, повышение привилегий используется точечно и только для тех операций, где это реально необходимо.
**Приоритет эскалаторов:** `sudo`, `doas`, `pkexec`, `run0` (механизм настройки приоритета пользователем будет реализован позже).

**Сценарии использования:**

1.  **Монтирование (`mount`):**
    * **БЕЗ SUDO.** Используется `unshare(CLONE_NEWUSER)` и нативные syscalls внутри namespace.

2.  **Шифрование (`cryptsetup`):**
    * **С SUDO.** Так как требуется доступ к Device Mapper, утилита вызывает внешний процесс: `sudo cryptsetup open ...`.
3.  **Чтение защищенных файлов (Permission Denied):**
    * **С SUDO.** Если утилита встречает файл, который не может прочитать (например, `/etc/shadow`), она должна запросить у пользователя права.
    * *Примечание:* Если решить проблему "на месте" запросом на повышение привилегий без отказа от Zero-Copy невозможно (то есть без физического копирования файла), то тогда допустимо завершиться с ошибкой, выполнив очистку ресурсов (аналог trap или его реализация в Rust), и попросить пользователя перезапустить команду от имени root.

4.  **Запись/Восстановление в системные папки:**
    * **С SUDO.** При команде `unfreeze` (например, в `/etc/`), утилита использует `sudo rsync` для финальной записи файлов.

При каждом запросе на повышение привилегий утилита чётко объясняет пользователю (на английском языке), для какой именно цели они запрашиваются.

### Технический Стек и Зависимости

Проект использует **Rust Edition 2024**.
Строго придерживайся следующих библиотек (уже прописаны в `Cargo.toml`):

* **CLI интерфейс:** `clap` (версия 4+, features = ["derive"]). Используй декларативный подход (struct/enum).
* **Обработка ошибок:** `anyhow` (для приложений) и опционально `thiserror` (добавь в Cargo.toml только если потребуется).
* **Конфигурация:** `serde` + `serde_yaml` (для парсинга манифестов бэкапов).
* **UI/UX:** `indicatif` (прогресс-бары), `log` + `env_logger` (структурированное логирование).
* **Система:** `std::process::Command` (запуск внешних утилит), `which` (поиск бинарников), `tempfile` (безопасные временные файлы).
* **Тесты:** `assert_cmd`, `predicates` (интеграционные тесты CLI).

### Архитектурные Принципы

#### Внешние Утилиты (Runtime Dependencies)

Мы **не** переписываем SquashFS или LUKS на Rust. Мы управляем ими.
Утилита должна полагаться на наличие в системе:

* `mksquashfs` / `unsquashfs`
* `cryptsetup` (требует root через sudo или doas)
* `tar2sqfs` (опционально)
* `rclone`

#### Два режима работы (Взаимоисключающие)

1. **Secure Mode (LUKS):**
* Создание архива -> Шифрование через `cryptsetup` (в файл-контейнер) -> Запись на диск.
* Требует свободного места на локальном диске или монтирования (FUSE).


2. **Streaming Mode (`--direct-rclone`):**
* Пайплайн: `mksquashfs (stdout) | rclone rcat (stdin)`.
* **Нулевая запись на диск ноутбука.**
* Несовместим с LUKS (на данном этапе).

При этом возможна конечно и работа и без одного из этих режимов: просто создание сжатого незашифрованно SquashFS-образа в указанном каталоге (смонтированном или нет). Назовём этот режим и без LUKS или без Rclone как "plain", соответсветно режим без Rclone запись файла в каталог локальной ФС, а не на удалённую машину по сети это "plain/LUKS". Пока что в fish-функциях реализован имеено он. 

Streaming Mode через Rclone в fish-функциях пока не реализован. Его реализация запланирована в будущих версиях Rust-порта и будет осуществляться только после полного переноса текущего функциона из Fish в Rust. При этом писать код порта Rust нужно с учётом того, что такая возмонжность будет добавлена в дальнейшем.


#### Тестирование и Mocking (Критически важно)

* **Unit-тесты:** Вся логика, которая *формирует* команды, должна быть протестирована без реального запуска.
* Используй паттерн `CommandExecutor` (Trait), чтобы подменять реальный вызов `std::process::Command` на Mock-объект в тестах. Мы должны проверять, *правильные ли флаги* мы передаем в `cryptsetup`, не запуская его.

### Методология разработки (Strict TDD)

Разработка ведется строго по методологии **Test Driven Development (Red-Green-Refactor)**.
Ты не имеешь права писать реализацию функционала, пока под него не написан unit-тест.

**Алгоритм работы над любой задачей:**

1.  **Создай тест (Red):**
    * Напиши unit-тест, который описывает желаемое поведение (например, "функция должна вернуть корректную строку команды `mksquashfs`").
    * Убедись, что тест не проходит (или код не компилируется, так как функции еще нет).
2.  **Реализуй минимум (Green):**
    * Напиши минимально необходимый код, чтобы тест прошел. Не пиши лишней логики "про запас".
3.  **Улучши (Refactor):**
    * Приведи код в соответствие со стандартами стиля, убери дублирование, но следи, чтобы тесты оставались зелеными.

**Специфика TDD и уровни тестирования:**

Поскольку мы работаем с внешними утилитами, важно четко разделять типы тестов:

1.  **Unit-тесты (в папке `src/`):**
    * **Цель:** Проверка внутренней логики (парсинг аргументов, формирование команд, обработка YAML).
    * **Запрет:** Здесь **ЗАПРЕЩЕНО** запускать реальные внешние процессы (`mksquashfs`, `cryptsetup`) или выполнять долгие операции ввода-вывода.
    * **Инструмент:** Используй Mock-объекты (паттерн `CommandExecutor`), чтобы проверить, что Rust-код *намеревается* запустить правильную команду с правильными флагами. Мы тестируем намерения, а не побочные эффекты.
2.  **Интеграционные тесты (в папке `tests/`):**
    * **Цель:** Проверка реальной работы ("Smoke Tests" на маленьких файлах).
    * **Разрешение:** Здесь **НУЖНО** работать с реальными файлами (через `tempfile`) и запускать реальные бинарники.
    * **Приоритет:** Такие тесты пишутся после того, как Unit-тесты прошли (Green).

**Правило:** Если в твоем ответе есть код функции, но нет кода теста к ней (или упоминания, что тест уже был написан ранее) — это нарушение инструкции.

* **Integration-тесты:** Используй `tests/` для реальных "дымных" тестов (Smoke Tests) на маленьких файлах (10-20KB), чтобы убедиться, что пайплайн работает end-to-end.

### Стандарты Кода (Coding Standards)

1. **Безопасность (Safety):**
* **ЗАПРЕЩЕНО:** Использовать `.unwrap()` или `.expect()` в основном коде (кроме тестов). Все ошибки должны пробрасываться через `Result` и `?`.
* Все пути к файлам должны обрабатываться корректно (пробелы, спецсимволы).


2. **Стиль:**
* Используй `rustfmt`.
* Комментарии пиши на **русском языке** (команда разработчиков русскоязычная).
* Справочную информацию и любой взаимодействие с конечным пользователем сохрани на английском
* Имена функций и переменных — на английском, в `snake_case`.


3.  **Логирование и UI:**
* `stdout`: Используется ТОЛЬКО для финального результата (путь к созданному файлу, JSON-отчет) или потока данных (если запрошен pipe).
* `stderr`: Используется для ВСЕГО остального: логи, ошибки, интерактивные вопросы (пароли), прогресс-бары. Это критично для корректной работы перенаправлений (`> log.txt`).


### Работа с MCP и Контекстом

* **Context7:** Если ты не уверен в синтаксисе новой версии библиотеки (например, `clap v4` сильно отличается от `v3`), **ОБЯЗАТЕЛЬНО** используй MCP `context7` для получения актуальной документации и примеров перед написанием кода. Не галлюцинируй устаревшим синтаксисом. Но если вся необходимая документация у тебя уже есть в контексте предыдущих сообщений, не запрашивай документацию, с целью экономии запросов. 


### Возможный вариант структуры проекта

```
src/
├── lib.rs              # Общая библиотека (шаринг кода между утилитами), если понадобиться
├── bin/
│   ├── zks-rs.rs       # Точка входа для основной утилиты (CLI zks)
│   └── squash_manager-rs.rs # Точка входа для менеджера (CLI squash_manager)
├── engine.rs           # Реализация команд (freeze, unfreeze и т.д.) и парсинг агрументов через оркестрацию внешних процессов
├── executor.rs         # Абстракция CommandExecutor над std::process::Command (Real vs Mock)
├── manifest.rs         # Работа с YAML
└── utils.rs            # # Мелкие утилиты (проверка root, поиск путей)
```

### Работа с замисимостями

Если потребуются какие либо зависимости, добавляй их в `Cargo.toml`.

### Работа с TODO.md (Progress Tracking)

Файл [TODO.md](TODO.md) является **Единственным Источником Истины** (SSOT) о прогрессе проекта.

1.  **Чтение:** Перед началом работы всегда читай `TODO.md`, чтобы понять текущий контекст.
2.  **Запись:**
    * Ты ОБЯЗАН отмечать выполненные задачи, меняя `[ ]` на `[x]`, но **только** после того, как тесты (Integration Tests) подтвердили работоспособность фичи.
    * **ЗАПРЕЩЕНО** добавлять в `TODO.md` мелкие промежуточные подзадачи (subtasks), которые ты генерируешь в процессе "размышления" (Chain of Thought). Оставь `TODO.md` чистым и высокоуровневым.
    * Если ты понимаешь, что в плане не хватает критически важного крупного этапа, ты можешь предложить пользователю добавить его, но не меняй структуру файла самовольно.
    * задачи из TODO.md ты можешь разбивать на более мелкие в своих собственных TODO-листах при планировании



### Чек-лист перед генерацией кода

1. Понял ли я, какой режим (LUKS/plain или Rclone Stream) требуется в этом куске кода?
2. Проверил ли я актуальность API библиотеки через Context7?
3. Обработал ли я возможные ошибки (нет места, нет сети, нет прав root)?
4. Безопасность тестов: Убедился ли я, что тесты не пытаются выполнять деструктивные команды (luksFormat, rm -rf) на реальных путях пользователя? (Надо использовать Mock-объекты или временные файлы (tempfile).)


